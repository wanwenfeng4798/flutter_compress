/// Stub so web (and other `dart.library.html` targets) never load the
/// `dart:io` FFmpeg backend. Real implementation: [FlutterCompressDesktop]
/// in `flutter_compress_pro_desktop.dart`.
class FlutterCompressDesktop {
  static void registerWith() {}
}
